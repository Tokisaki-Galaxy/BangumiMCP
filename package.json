{
  "name": "bangumi-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "Bangumi MCP Python Worker",
  "scripts": {
    "build": "uv run pywrangler sync",
    "dev": "uv run pywrangler dev",
    "deploy": "uv run pywrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
